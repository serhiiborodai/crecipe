# 🚀 Полная инструкция по настройке ChefRecipes

Эта инструкция проведёт тебя через все шаги настройки проекта.

---

## 📋 Содержание

1. [Firebase — авторизация и база данных](#1-firebase)
2. [Stripe — приём платежей](#2-stripe)
3. [Vimeo — хостинг видео](#3-vimeo)
4. [Настройка .env.local](#4-env)
5. [Запуск проекта](#5-запуск)
6. [Админ-панель](#6-админ-панель)
7. [Деплой на Vercel](#7-деплой)

---

## 1. Firebase

Firebase используется для:
- Авторизации через Google
- Хранения рецептов и покупок в Firestore

### Шаг 1.1: Создание проекта

1. Открой [console.firebase.google.com](https://console.firebase.google.com/)
2. Войди в Google аккаунт
3. Нажми **"Создать проект"** (Create a project)
4. Введи название: `chef-recipes` (или любое другое)
5. Google Analytics — можно отключить (не нужен)
6. Нажми **"Создать проект"**
7. Дождись создания → **"Продолжить"**

### Шаг 1.2: Включение Google авторизации

1. В левом меню нажми **Authentication**
2. Нажми **"Начать"** (Get started)
3. Перейди на вкладку **"Sign-in method"**
4. В списке найди **Google** и нажми на него
5. Включи переключатель **"Включить"** (Enable)
6. В поле **"Адрес эл. почты для поддержки"** выбери свой email
7. Нажми **"Сохранить"**

✅ Google авторизация включена!

### Шаг 1.3: Создание базы данных Firestore

1. В левом меню нажми **Firestore Database**
2. Нажми **"Создать базу данных"** (Create database)
3. Выбери **"Запустить в рабочем режиме"** (Start in production mode)
4. Нажми **"Далее"**
5. Выбери регион:
   - Для России/СНГ: `europe-west1` (Бельгия) или `europe-central2` (Варшава)
   - Для Европы: `europe-west1`
6. Нажми **"Включить"**

### Шаг 1.4: Настройка правил Firestore

1. В Firestore Database перейди на вкладку **"Правила"** (Rules)
2. Удали всё и вставь этот код:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Рецепты - читать могут все
    match /recipes/{recipeId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    
    // Настройки сайта - читать могут все
    match /settings/{settingId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    
    // Пользователи - только свои данные
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Покупки - читать только свои
    match /purchases/{purchaseId} {
      allow read: if request.auth != null && resource.data.userId == request.auth.uid;
      allow create: if false; // создаются только через webhook
    }
  }
}
```

3. Нажми **"Опубликовать"** (Publish)

### Шаг 1.5: Получение ключей для веб-приложения

1. Нажми на ⚙️ **шестерёнку** рядом с "Project Overview" → **"Настройки проекта"**
2. Прокрути вниз до секции **"Ваши приложения"**
3. Нажми на иконку **</>** (Web)
4. Введи название: `chef-recipes-web`
5. Галочку "Firebase Hosting" **НЕ ставь**
6. Нажми **"Зарегистрировать приложение"**
7. Появится код — скопируй значения:

```javascript
const firebaseConfig = {
  apiKey: "AIzaSy...",                    // 👈 NEXT_PUBLIC_FIREBASE_API_KEY
  authDomain: "xxx.firebaseapp.com",      // 👈 NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
  projectId: "xxx",                       // 👈 NEXT_PUBLIC_FIREBASE_PROJECT_ID
  storageBucket: "xxx.appspot.com",       // 👈 NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
  messagingSenderId: "123456789",         // 👈 NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
  appId: "1:123:web:abc..."               // 👈 NEXT_PUBLIC_FIREBASE_APP_ID
};
```

8. Нажми **"Продолжить в консоль"**

### Шаг 1.6: Создание Service Account (для Stripe webhook)

1. В настройках проекта перейди на вкладку **"Сервисные аккаунты"** (Service accounts)
2. Убедись, что выбран **Firebase Admin SDK**
3. Нажми **"Создать закрытый ключ"** (Generate new private key)
4. Подтверди нажав **"Создать ключ"**
5. Скачается JSON файл (например `chef-recipes-firebase-adminsdk-xxx.json`)
6. Открой этот файл в текстовом редакторе
7. Скопируй **ВСЁ содержимое** — это будет `FIREBASE_SERVICE_ACCOUNT_KEY`

⚠️ **Важно:** Этот JSON нужно вставить в .env.local **одной строкой** без переносов!

---

## 2. Stripe

Stripe используется для приёма платежей.

### Шаг 2.1: Регистрация

1. Открой [dashboard.stripe.com](https://dashboard.stripe.com/)
2. Нажми **"Создать аккаунт"** или войди
3. Заполни данные (для тестового режима можно не подтверждать бизнес)

### Шаг 2.2: Получение API ключей

1. В левом меню нажми **Developers** → **API keys**
2. Ты увидишь два ключа:
   - **Publishable key** (начинается с `pk_test_...`) → `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`
   - **Secret key** (нажми "Reveal" — начинается с `sk_test_...`) → `STRIPE_SECRET_KEY`

⚠️ **Внимание:** Используй **тестовые ключи** (с `_test_`) для разработки!

### Шаг 2.3: Настройка Webhook (после деплоя)

Webhook нужен чтобы записывать покупки в базу после оплаты.

**Для локальной разработки:**

1. Установи [Stripe CLI](https://stripe.com/docs/stripe-cli):
   ```bash
   # macOS
   brew install stripe/stripe-cli/stripe
   
   # или скачай с https://github.com/stripe/stripe-cli/releases
   ```

2. Авторизуйся:
   ```bash
   stripe login
   ```

3. Запусти форвардинг:
   ```bash
   stripe listen --forward-to localhost:3000/api/webhooks/stripe
   ```

4. Stripe CLI покажет **webhook signing secret** (начинается с `whsec_...`)
5. Скопируй его в `STRIPE_WEBHOOK_SECRET`

**Для production (после деплоя):**

1. В Stripe Dashboard → **Developers** → **Webhooks**
2. Нажми **"Add endpoint"**
3. Endpoint URL: `https://твой-домен.vercel.app/api/webhooks/stripe`
4. В "Select events" выбери: `checkout.session.completed`
5. Нажми **"Add endpoint"**
6. Скопируй **Signing secret** → `STRIPE_WEBHOOK_SECRET`

### Шаг 2.4: Тестовые карты

Для тестирования оплаты используй:

| Сценарий | Номер карты | CVC | Дата |
|----------|-------------|-----|------|
| ✅ Успешная оплата | `4242 4242 4242 4242` | любые 3 цифры | любая будущая |
| ❌ Отклонена | `4000 0000 0000 0002` | любые 3 цифры | любая будущая |
| ⚠️ Требует 3D Secure | `4000 0027 6000 3184` | любые 3 цифры | любая будущая |

---

## 3. Vimeo

Vimeo используется для хостинга платных видео.

### Шаг 3.1: Регистрация

1. Открой [vimeo.com](https://vimeo.com/)
2. Нажми **"Join"** и зарегистрируйся
3. Бесплатного плана хватит для начала (до 500 МБ/неделю)

### Шаг 3.2: Загрузка видео

1. Нажми **"New video"** → **"Upload"**
2. Выбери файл и загрузи
3. Заполни название и описание

### Шаг 3.3: Настройка приватности

1. Открой загруженное видео
2. Нажми **"Settings"** (или ⚙️)
3. Перейди в **"Privacy"**
4. Выбери один из вариантов:
   - **"Hide from Vimeo"** — видео не видно на Vimeo, но работает embed ✅
   - **"Only people with the private link"** — нужна ссылка
5. В разделе **"Where can this video be embedded?"** выбери **"Specific domains"**
6. Добавь свой домен: `localhost`, `твой-домен.vercel.app`

### Шаг 3.4: Получение Video ID

1. Открой видео на Vimeo
2. Посмотри на URL: `https://vimeo.com/76979871`
3. Число в конце — это **Video ID**: `76979871`
4. Используй этот ID в админке при создании рецепта

---

## 4. Настройка .env.local {#4-env}

Создай файл `.env.local` в корне проекта:

```bash
# В терминале:
cp env.example .env.local

# Или создай файл вручную
```

Заполни его своими данными:

```env
# ===== FIREBASE (Client-side) =====
# Из шага 1.5 — конфигурация веб-приложения
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyB...
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=chef-recipes-xxx.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=chef-recipes-xxx
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=chef-recipes-xxx.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789012
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789012:web:abcdef123456

# ===== FIREBASE ADMIN (Server-side) =====
# Из шага 1.6 — весь JSON одной строкой!
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"chef-recipes-xxx","private_key_id":"abc123","private_key":"-----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----\n","client_email":"firebase-adminsdk-xxx@chef-recipes-xxx.iam.gserviceaccount.com","client_id":"123456789","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"https://www.googleapis.com/robot/v1/metadata/x509/firebase-adminsdk-xxx%40chef-recipes-xxx.iam.gserviceaccount.com"}

# ===== STRIPE =====
# Из шага 2.2 — API ключи
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_51...
STRIPE_SECRET_KEY=sk_test_51...
# Из шага 2.3 — Webhook secret
STRIPE_WEBHOOK_SECRET=whsec_...

# ===== АДМИНЫ =====
# Email адреса через запятую (без пробелов после запятой)
# Эти пользователи будут иметь доступ к /admin
NEXT_PUBLIC_ADMIN_EMAILS=твой-email@gmail.com

# ===== ПРИЛОЖЕНИЕ =====
# Для локальной разработки:
NEXT_PUBLIC_BASE_URL=http://localhost:3000
# Для production замени на реальный домен:
# NEXT_PUBLIC_BASE_URL=https://chef-recipes.vercel.app
```

---

## 5. Запуск

### Первый запуск

```bash
# Установи зависимости (если ещё не установлены)
npm install

# Запусти dev сервер
npm run dev
```

Открой [http://localhost:3000](http://localhost:3000)

### Что должно работать

1. ✅ Открывается главная страница с лендингом
2. ✅ Кнопка "Войти через Google" → открывает popup авторизации
3. ✅ После входа показывается список рецептов
4. ✅ Если твой email в `NEXT_PUBLIC_ADMIN_EMAILS` — в шапке появляется "⚙️ Админ"

### Возможные проблемы

**Ошибка Firebase при входе:**
- Проверь, что Google авторизация включена в Firebase Console
- Проверь правильность ключей в `.env.local`
- Добавь `localhost` в авторизованные домены: Firebase Console → Authentication → Settings → Authorized domains

**Ошибка Firestore:**
- Проверь, что база данных создана
- Проверь, что правила опубликованы

---

## 6. Админ-панель

### Доступ

1. Добавь свой Google email в `NEXT_PUBLIC_ADMIN_EMAILS`
2. Перезапусти сервер (`npm run dev`)
3. Войди через Google
4. В шапке появится ссылка **"⚙️ Админ"**
5. Или открой напрямую: [http://localhost:3000/admin](http://localhost:3000/admin)

### Создание рецепта

1. Перейди в **Админ** → **Рецепты** → **Добавить рецепт**
2. Заполни поля:
   - **Название** — например "Идеальный стейк"
   - **Краткое описание** — для карточки на главной
   - **Полное описание** — подробное описание курса
   - **Категория** — Мясо, Паста, Десерты и т.д.
   - **Сложность** — Легко / Средне / Сложно
   - **Время приготовления** — например "30 минут"
   - **Цена** — в рублях (например 999)
   - **YouTube промо** — ссылка на промо-ролик (опционально)
   - **Ингредиенты** — через запятую
3. Добавь видео:
   - Нажми **"+ Добавить видео"**
   - Введи название части
   - Вставь **Vimeo ID** (только число!)
   - Добавь описание (опционально)
4. Нажми **"Сохранить черновик"** или **"Опубликовать"**

### Настройки сайта

В разделе **Настройки сайта** можно изменить:
- Заголовок на главной странице
- Описание
- Преимущества (блоки с иконками)
- Текст в футере

---

## 7. Деплой на Vercel

### Шаг 7.1: Подготовка

1. Залей код в GitHub репозиторий
2. Зарегистрируйся на [vercel.com](https://vercel.com/)

### Шаг 7.2: Импорт проекта

1. В Vercel нажми **"Add New..."** → **"Project"**
2. Выбери свой GitHub репозиторий
3. Vercel автоматически определит Next.js

### Шаг 7.3: Переменные окружения

1. Перед деплоем нажми **"Environment Variables"**
2. Добавь **ВСЕ** переменные из `.env.local`:
   - `NEXT_PUBLIC_FIREBASE_API_KEY`
   - `NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN`
   - `NEXT_PUBLIC_FIREBASE_PROJECT_ID`
   - `NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET`
   - `NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID`
   - `NEXT_PUBLIC_FIREBASE_APP_ID`
   - `FIREBASE_SERVICE_ACCOUNT_KEY`
   - `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`
   - `STRIPE_SECRET_KEY`
   - `STRIPE_WEBHOOK_SECRET`
   - `NEXT_PUBLIC_ADMIN_EMAILS`
   - `NEXT_PUBLIC_BASE_URL` — **замени на Vercel URL!**

3. Нажми **"Deploy"**

### Шаг 7.4: После деплоя

1. Скопируй URL проекта (например `https://chef-recipes.vercel.app`)

2. **Обнови `NEXT_PUBLIC_BASE_URL`:**
   - Vercel → Settings → Environment Variables
   - Измени `NEXT_PUBLIC_BASE_URL` на реальный URL
   - Redeploy проект

3. **Добавь домен в Firebase:**
   - Firebase Console → Authentication → Settings → Authorized domains
   - Добавь `chef-recipes.vercel.app`

4. **Настрой Stripe Webhook:**
   - Stripe Dashboard → Developers → Webhooks
   - Add endpoint: `https://chef-recipes.vercel.app/api/webhooks/stripe`
   - Select events: `checkout.session.completed`
   - Скопируй Signing secret и обнови `STRIPE_WEBHOOK_SECRET` в Vercel

5. **Добавь домен в Vimeo:**
   - Vimeo → Video Settings → Privacy → Embed → Specific domains
   - Добавь `chef-recipes.vercel.app`

---

## ✅ Чеклист

- [ ] Firebase проект создан
- [ ] Google авторизация включена
- [ ] Firestore база создана
- [ ] Правила Firestore опубликованы
- [ ] Stripe аккаунт создан
- [ ] API ключи Stripe получены
- [ ] Vimeo аккаунт создан
- [ ] `.env.local` заполнен
- [ ] Проект запускается локально
- [ ] Вход через Google работает
- [ ] Админ-панель доступна
- [ ] (После деплоя) Webhook Stripe настроен
- [ ] (После деплоя) Домены добавлены в Firebase и Vimeo

---

## 🆘 Помощь

Если что-то не работает:

1. Проверь консоль браузера (F12 → Console)
2. Проверь терминал с запущенным `npm run dev`
3. Убедись, что все переменные в `.env.local` заполнены правильно
4. Перезапусти сервер после изменения `.env.local`

**Частые ошибки:**

| Ошибка | Решение |
|--------|---------|
| `Firebase: Error (auth/configuration-not-found)` | Проверь FIREBASE_API_KEY и AUTH_DOMAIN |
| `Firebase: Error (auth/unauthorized-domain)` | Добавь localhost в Authorized domains |
| Страница не загружается | Проверь консоль на ошибки, перезапусти сервер |
| Нет доступа к админке | Проверь email в NEXT_PUBLIC_ADMIN_EMAILS |

