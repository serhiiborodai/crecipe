# 🚀 Production Deployment Guide

Инструкция по деплою проекта на Vercel и настройке Stripe для продакшена.

---

## 📋 Содержание

1. [Подготовка к деплою](#1-подготовка-к-деплою)
2. [Деплой на Vercel](#2-деплой-на-vercel)
3. [Настройка Stripe для продакшена](#3-настройка-stripe-для-продакшена)
4. [Настройка Firebase для продакшена](#4-настройка-firebase-для-продакшена)
5. [Финальная проверка](#5-финальная-проверка)
6. [Переход на Live платежи](#6-переход-на-live-платежи)

---

## 1. Подготовка к деплою

### Убедись что локально всё работает:
- [ ] Авторизация через Google работает
- [ ] Тестовая оплата проходит
- [ ] После оплаты видео становится доступным
- [ ] Админ-панель работает

### Подготовь репозиторий:
```bash
# Убедись что .env.local в .gitignore (не должен попасть в репозиторий!)
cat .gitignore | grep .env

# Запуш код на GitHub
git add .
git commit -m "Ready for production"
git push origin main
```

---

## 2. Деплой на Vercel

### Шаг 1: Создай проект на Vercel

1. Зайди на [vercel.com](https://vercel.com) и авторизуйся через GitHub
2. Нажми **"Add New..."** → **"Project"**
3. Выбери репозиторий с проектом
4. Vercel автоматически определит Next.js

### Шаг 2: Настрой Environment Variables

В разделе **"Environment Variables"** добавь все переменные:

#### Firebase (клиентские):
```
NEXT_PUBLIC_FIREBASE_API_KEY=твой_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=твой_проект.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=твой_проект
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=твой_проект.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abcdef
```

#### Firebase (серверные):
```
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account",...весь JSON в одну строку...}
```

⚠️ **Важно:** JSON сервисного аккаунта должен быть в одну строку без переносов!

#### Stripe (пока Test ключи):
```
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_... (получим после создания webhook)
```

#### Админы:
```
NEXT_PUBLIC_ADMIN_EMAILS=твой@gmail.com,другой@gmail.com
```

### Шаг 3: Деплой

1. Нажми **"Deploy"**
2. Дождись завершения билда
3. Получи URL: `https://твой-проект.vercel.app`

---

## 3. Настройка Stripe для продакшена

### ⚠️ Важно: Stripe CLI НЕ работает на продакшене!

На локалке мы использовали `stripe listen` для пересылки webhook'ов. На продакшене нужно настроить webhook напрямую в Stripe Dashboard.

### Шаг 1: Создай Webhook Endpoint

1. Открой [dashboard.stripe.com/webhooks](https://dashboard.stripe.com/webhooks)
2. Убедись что ты в **Test mode** (переключатель вверху справа)
3. Нажми **"+ Add destination"**

### Шаг 2: Настрой Webhook

1. Выбери **"Webhook"**
2. В поле **"Endpoint URL"** введи:
   ```
   https://твой-проект.vercel.app/api/webhooks/stripe
   ```
3. В разделе **"Select events to listen to"** нажми **"+ Select events"**
4. Найди и выбери:
   - `checkout.session.completed`
5. Нажми **"Add events"**
6. Нажми **"Add destination"**

### Шаг 3: Получи Webhook Secret

1. После создания webhook'а, нажми на него в списке
2. Найди раздел **"Signing secret"**
3. Нажми **"Reveal"**
4. Скопируй секрет (начинается с `whsec_...`)

### Шаг 4: Обнови переменную в Vercel

1. Зайди в проект на Vercel
2. **Settings** → **Environment Variables**
3. Найди `STRIPE_WEBHOOK_SECRET`
4. Замени значение на новый секрет из Dashboard
5. Нажми **"Save"**

### Шаг 5: Редеплой

```bash
# Или в Vercel Dashboard: Deployments → ... → Redeploy
vercel --prod
```

Или в интерфейсе Vercel:
1. **Deployments** → последний деплой
2. **...** → **"Redeploy"**

---

## 4. Настройка Firebase для продакшена

### Добавь домен Vercel в Firebase

1. Открой [Firebase Console](https://console.firebase.google.com)
2. Выбери проект
3. **Authentication** → **Settings** → **Authorized domains**
4. Нажми **"Add domain"**
5. Добавь:
   - `твой-проект.vercel.app`
   - Если есть кастомный домен — его тоже

### Обнови правила Firestore (опционально)

Для продакшена рекомендуется более строгие правила безопасности:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Рецепты - читать могут все, писать только админы
    match /recipes/{recipeId} {
      allow read: if true;
      allow write: if request.auth != null && 
        request.auth.token.email in ['твой@gmail.com'];
    }
    
    // Покупки - пользователь видит только свои
    match /purchases/{odocument=**} {
      allow read: if request.auth != null;
      allow write: if false; // Только через webhook
    }
    
    // Настройки сайта
    match /settings/{document=**} {
      allow read: if true;
      allow write: if request.auth != null && 
        request.auth.token.email in ['твой@gmail.com'];
    }
  }
}
```

---

## 5. Финальная проверка

### Чеклист после деплоя:

- [ ] Сайт открывается по URL Vercel
- [ ] Google авторизация работает
- [ ] Рецепты отображаются
- [ ] Кнопка "Купить" открывает Stripe Checkout
- [ ] После тестовой оплаты видео становится доступным
- [ ] Админ-панель доступна для админских email'ов

### Тестовая оплата на продакшене:

Используй тестовую карту:
```
Номер: 4242 4242 4242 4242
Дата: любая в будущем
CVC: любые 3 цифры
```

### Проверка webhook'а:

1. В Stripe Dashboard → **Webhooks** → твой endpoint
2. Внизу раздел **"Webhook attempts"**
3. После тестовой оплаты должен появиться успешный запрос (статус 200)

Если статус не 200:
- Проверь что `STRIPE_WEBHOOK_SECRET` правильный
- Проверь логи в Vercel: **Deployments** → **Functions** → **api/webhooks/stripe**

---

## 6. Переход на Live платежи

Когда готов принимать реальные деньги:

### Шаг 1: Активируй аккаунт Stripe

1. В Stripe Dashboard заполни все данные о бизнесе
2. Пройди верификацию
3. Подключи банковский счёт для выплат

### Шаг 2: Получи Live ключи

1. В Stripe Dashboard переключись на **Live mode** (переключатель вверху)
2. **Developers** → **API keys**
3. Скопируй:
   - `Publishable key` (pk_live_...)
   - `Secret key` (sk_live_...)

### Шаг 3: Создай Live Webhook

1. В **Live mode** → **Webhooks** → **"+ Add destination"**
2. URL тот же: `https://твой-проект.vercel.app/api/webhooks/stripe`
3. События: `checkout.session.completed`
4. Скопируй новый **Signing secret**

### Шаг 4: Обнови переменные в Vercel

Замени Test ключи на Live:

```
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_...
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_... (новый, из Live webhook)
```

### Шаг 5: Редеплой и проверь

```bash
vercel --prod
```

⚠️ **После перехода на Live режим тестовые карты НЕ работают!**

---

## 🔧 Troubleshooting

### Проблема: Webhook возвращает ошибку 500

**Причина:** Неправильный `STRIPE_WEBHOOK_SECRET`

**Решение:**
1. Убедись что используешь секрет из **Dashboard**, а не из Stripe CLI
2. Секрет должен быть от того же webhook endpoint

### Проблема: После оплаты видео не открывается

**Причина:** Webhook не записывает покупку в базу

**Решение:**
1. Проверь Stripe Dashboard → Webhooks → webhook attempts
2. Проверь логи функции в Vercel
3. Убедись что Firebase настроен правильно

### Проблема: Google авторизация не работает

**Причина:** Домен не добавлен в Firebase

**Решение:**
1. Firebase Console → Authentication → Settings → Authorized domains
2. Добавь домен Vercel

### Проблема: Ошибка CORS при оплате

**Причина:** Stripe ключи неправильные или отсутствуют

**Решение:**
1. Проверь что все NEXT_PUBLIC_* переменные добавлены в Vercel
2. Сделай редеплой после добавления переменных

---

## 📊 Мониторинг

### Vercel
- **Analytics** — трафик и производительность
- **Logs** — логи функций (API routes)

### Stripe
- **Payments** — все платежи
- **Customers** — клиенты
- **Webhooks** — статусы webhook'ов

### Firebase
- **Authentication** → **Users** — зарегистрированные пользователи
- **Firestore** — данные о покупках и рецептах

---

## 🎉 Готово!

После выполнения всех шагов твоя платформа готова принимать платежи!

Если возникнут вопросы — проверь логи в Vercel и статусы webhook'ов в Stripe.

